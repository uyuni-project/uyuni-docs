= {productname} Server Upgrade
:revdate: 2026-06-17
:page-revdate: {revdate}

Before running the upgrade command, it is required to update the host operating system.
Updating the host operating system will also result in the update of the {productname} tooling such as the `mgradm` tool.

[IMPORTANT]
====
Before upgrading, verify that every root and intermediate CA certificate used by {productname} marks `X509v3 Basic Constraints` as `critical` and includes `CA:TRUE`.
Strict certificate validators, including Python 3.13 and later, reject CA certificates that miss the `critical` flag.

If your installation uses an older self-signed CA generated by {productname}, you might need to create a new CA and new server certificates, then deploy the new root CA to all clients.
For the verification command, certificate replacement, and client root-CA deployment procedure, see xref:administration:ssl-certs-imported.adoc#ssl-certs-verify-ca-basic-constraints[], xref:administration:ssl-certs-selfsigned.adoc#ssl-certs-selfsigned-create-replace[], and xref:administration:ssl-certs-imported.adoc#ssl-certs-import-deploy-root-ca[].
====

.Procedure: Upgrading Server
[role=procedure]
____

. Refresh software repositories with `zypper`:

+

[source,shell]
----
zypper ref
----

ifeval::[{mlm-content} == true]

. Depending on the host operating system, proceed with these steps:

+

For a transactional system such as {sl-micro}: ::

+

--

. Apply available updates with `transactional-update`:

+

[source,shell]
----
transactional-update
----

. If updates were applied, `reboot`.

--

+

endif::[]

ifeval::[{uyuni-content} == true]

. Apply available updates with `transactional-update`:

+

[source,shell]
----
transactional-update
----

. If updates were applied, `reboot`.

endif::[]

ifeval::[{mlm-content} == true]

For {sles}: ::

+

--

Update installed software with `zypper`:

[source,shell]
----
zypper up
----

--

endif::[]

+

. The {productname} Server container can be updated using the following command:

+

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

+

[source,shell]
----
mgradm upgrade podman
----

+

This command will bring the status of the container up-to-date and restart the server.

+
. Clean up the unused container images to free disk space:

+
[source,shell]
----
podman image prune -a
----

+

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-check-rpmnew-rpmsave.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-check-rpmnew-rpmsave.adoc[]
endif::[]

____


.Upgrading with third-party SSL certificate
[NOTE]
====
In previous versions, the database container needed to have an SSL certificate with the following Subject Alternate Names (SANs):

* `db`
* `reportdb`

This is no longer required.
The database container needs only the externally facing fully qualified domain name.
The old certificates with `db` and `reportdb` SANs can be still used.

The same certificate can be used for both the main container and the database one.

In order to pass the new certificates to the upgrade command, use the `--ssl-db-ca-root`, `--ssl-db-cert` and `--ssl-db-key` parameters.
====

.Upgrading to specific version
[NOTE]
====
If you do not specify the tag parameter, it will default to upgrading to the most recent version.
To upgrade to a specific version, provide the tag parameter with the desired image tag.
====

For more information on the upgrade command and its parameters, use the following command:

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-ptf-loss-during-upgrade.adoc[]
endif::[]

[source,shell]
----
mgradm upgrade podman -h
----


For air-gapped installations, first upgrade the container RPM packages, then run the `mgradm` command.



ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-database-backup-volume.adoc[leveloffset=+1]
endif::[]

ifdef::backend-pdf[]
include::../../partials/snippet-database-backup-volume.adoc[leveloffset=+1]
endif::[]
