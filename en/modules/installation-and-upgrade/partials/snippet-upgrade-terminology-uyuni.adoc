== Terminology

This guide uses the following terms consistently:

Product upgrade::
Moving to a newer {productname} release.
Update the host operating system, then run `mgradm upgrade podman` or `mgrpxy upgrade podman`.

Migration::
Changing the deployment form, for example moving a legacy RPM-based installation to containers.

{productname} releases are rolling and have no separate maintenance-update concept.
