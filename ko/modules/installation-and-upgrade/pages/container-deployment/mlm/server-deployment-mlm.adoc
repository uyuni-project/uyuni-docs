[[deploy-mlm-server]]
= {productname} {productnumber} 서버 배포
:description: SL Micro 6.1, SUSE Linux Enterprise Server 15 SP7 또는 기타 호환 시스템에서 SUSE Multi-Linux Manager 5.1 서버를 구성하여 여러 Linux를 관리합니다.
:revdate: 2025-07-09
:page-revdate: {revdate}
:page-author: SUSE 제품 및 솔루션 문서팀
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1
ifeval::[{uyuni-content} == true]

:noindex:
endif::[]

이 가이드는 {productname} {productnumber} 컨테이너를 {sl-micro} {microversion} 또는 {sles} {bci-mlm}에 설치하고 구성하는 방법을 보여줍니다.

// Link to hardware requirements do not show them here. We list them on other pages.
// Done within next section.

== {productname}에 대한 하드웨어 요구사항

이 표는 귀하의 운영 체제 미설치 컴퓨터에 {productname} 서버를 배포하기 위한 소프트웨어 및 하드웨어 요구사항을 보여줍니다.
이 가이드의 목적을 위해 귀하의 머신은 16 GB의 RAM과 최소 200 GB의 디스크 공간을 가져야 합니다.
디스크 공간에 대한 배경 정보는 xref:installation-and-upgrade:hardware-requirements.adoc[]를 참조하십시오.

[cols="1,1", options="header"]
.소프트웨어 요구사항
|===
| 소프트웨어  | 권장
| 운영 체제       | {sl-micro} {microversion} 또는
 +
                           {sles} {bci-mlm}
|===




[cols="1,1", options="header"]
.하드웨어 요구사항
|===
| 아키텍처           | {x86_64}, {arm}, {s390x}, {ppc64le}
| 프로세서 (CPU)        | 최소 네 (4) 개의 64비트 CPU 코어
| RAM                    | 16 GB
| 디스크 공간             | 200 GB
| 채널 요구사항   | SUSE 또는 openSUSE 제품당 50 GB
 +
                           Red Hat 제품당 360 GB
| 스왑 공간:            | 8에서 12 GB
|===

.서버 컨테이너 호스트에 지원되는 운영 체제
[NOTE]
====
컨테이너 호스트에 지원되는 운영 체제는 {sl-micro} {microversion} 또는 {sles} {bci-mlm}입니다.

컨테이너 호스트::
컨테이너 호스트는 `Podman`과 같은 컨테이너 엔진이 장착된 서버로, 컨테이너를 관리하고 배포할 수 있게 해줍니다.
이 컨테이너는 응용 프로그램과 라이브러리와 같은 필수 구성 요소를 포함하지만 전체 운영 체제는 포함하지 않아 경량입니다.
이 설정은 응용 프로그램이 서로 다른 환경에서도 동일하게 실행되도록 보장합니다.
컨테이너 호스트는 이러한 컨테이너에 필요한 CPU, 메모리 및 저장소와 같은 자원을 제공합니다.
====


[IMPORTANT]
====
서버 배포는 완전한 도메인 이름(FQDN)의 사용을 요구합니다.
라우터나 네트워크에서 FQDN의 자동 DNS 제공이 없으면 배포 과정이 성공적으로 진행되지 않습니다.
FQDN은 일반적으로 <host>.<domain>.com 형식을 따릅니다.

예:

* `mlm.example.com`

* `mlm.container.lab`

자세한 정보는 xref:installation-and-upgrade:network-requirements.adoc[]의 네트워크 요구 사항 섹션을 참조하십시오.
====

== 영구 볼륨

// This sections needs to be run by devs. How would a customer use custom storage? How do we set this up with mgradm?

{productname} {productnumber}는 기본적으로 필요한 영구 저장소 볼륨을 정의합니다.
이들은 설치 중에, 이미 존재하지 않는 경우 `mgradm` 도구에 의해 생성됩니다.

이들 볼륨은 `/var/lib/containers/storage/volumes/`에 생성되며, 기본적으로 `Podman`는 이곳에 볼륨을 저장합니다.

.권장
[NOTE]
====
외부 저장 장치를 이 디렉토리에 마운트하여 저장소의 단순성을 활용할 수 있습니다.
이 {postgresql} 데이터베이스, 리포지토리의 바이너리 패키지, 캐시, 운영 체제 이미지, 자동 설치 배포 및 구성 파일을 저장할 것이므로 세 가지 권장 사항이 있습니다:

고속 스토리지::
이 탑재 지점은 이상적으로 NVMe 또는 SSD 클래스 장치여야 합니다.
느린 저장소는 {productname} 성능에 부정적인 영향을 미칩니다.

대용량::
이를 위한 최소 권장 크기는 300GB 이상이며, 관리해야 할 Linux 배포 또는 아키텍처가 여러 개일 경우 더 큰 크기가 권장됩니다.

권장 파일 시스템::
XFS(다만 {sl-micro} {microversion}에 대해 지원되는 파일 시스템은 모두 작동할 수 있습니다).

선택 사항::
볼륨 내부의 예상 경로(예: `/var/lib/containers/storage/volumes/var-spacewalk`)에 디스크를 마운트하여 사용자 지정 저장소를 제공할 수 있습니다.
이는 {productname} 배포의 복잡성을 더하며, 기본 저장소 권장 사항이 제공하는 복원력에 영향을 미칠 수 있습니다.

====


컨테이너의 모든 영구 볼륨 목록은 xref:installation-and-upgrade:container-management/persistent-container-volumes.adoc[]을 참조하십시오.




[[deploy-mlm-server-host]]
==  {productname} 서버 호스트 준비

{productname}을 {sl-micro} {microversion} 또는 {sles} {bci-mlm}에 배포할 수 있습니다. {sl-micro}은 트랜잭션 시스템이고, {sles}은 전체 서버 운영 체제입니다.

결정에 따라 xref:installation-and-upgrade:container-deployment/mlm/server-deployment-mlm.adoc#deploy-mlm-server-micro[] 또는 xref:installation-and-upgrade:container-deployment/mlm/server-deployment-mlm.adoc#deploy-mlm-server-sles[]로 계속 진행하고 선택하지 않은 섹션을 건너뜁니다.



[[deploy-mlm-server-micro]]
ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-prepare-micro-host.adoc[leveloffset=+1]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-prepare-micro-host.adoc[leveloffset=+1]
endif::[]

배포를 계속하려면 xref:installation-and-upgrade:container-deployment/mlm/server-deployment-mlm.adoc#deploy-mlm-server-persistent-storage[]을 참조하십시오.

[[deploy-mlm-server-sles]]
ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-prepare-sles-host.adoc[leveloffset=+1]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-prepare-sles-host.adoc[leveloffset=+1]
endif::[]

배포를 계속하려면 xref:installation-and-upgrade:container-deployment/mlm/server-deployment-mlm.adoc#deploy-mlm-server-persistent-storage[]을 참조하십시오.




[[deploy-mlm-server-persistent-storage]]
== 사용자 정의 영구 스토리지 구성

영구 스토리지를 구성하는 것은 선택 사항이지만, 이는 컨테이너의 디스크 가득 차는 문제를 피하는 유일한 방법입니다.
`mgr-storage-server` 도구를 사용하여 사용자 지정 영구 스토리지를 구성하는 것이 강력히 권장됩니다.

자세한 내용은 `mgr-storage-server --help`을 참조하십시오.

자세한 내용은 [command]\``mgr-storage-server \--help``를 참조하십시오.  이 도구는 컨테이너 스토리지 및 데이터베이스 볼륨 생성을 간소화합니다.

다음 방법으로 명령 사용:

----
mgr-storage-server <storage-disk-device> [<database-disk-device>]
----

예:

----
mgr-storage-server /dev/nvme1n1 /dev/nvme2n1
----

[NOTE]
====
이 명령은 `/var/lib/containers/storage/volumes`에 영구 스토리지 볼륨을 생성합니다.

자세한 내용은 다음을 참조하십시오.

* xref:installation-and-upgrade:container-management/persistent-container-volumes.adoc[]

* xref:administration:troubleshooting/tshoot-container-full-disk.adoc[]

====


[[deploy-mlm-server-mgradm]]
== {productname}를 `mgradm`와 함께 배포

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-note-deploy-certs.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-note-deploy-certs.adoc[]
endif::[]
ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-hardened-tmpdir.adoc[leveloffset=+2]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-hardened-tmpdir.adoc[leveloffset=+2]
endif::[]

.절차 `mgradm`을 사용하여 {productname}과 {productnumber}를 배포합니다.
. 루트로 로그인합니다.

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-step-deploy-podman-certs.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-step-deploy-podman-certs.adoc[]
endif::[]

+

[IMPORTANT]
====
실행된 명령이 실패하면 {productname} {productnumber}가 등록되었는지 확인하십시오.
설치 중에 등록을 건너뛰었고 이제 명령줄에서 등록해야 하는 경우, 레지스트리에 로그인하기 위한 아래 단계를 따르십시오:

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-login-into-suse-registry.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-login-into-suse-registry.adoc[]
endif::[]

프롬프트가 표시되면 {productname} {productnumber} 등록 키를 사용하십시오.
====

. 메시지가 표시되면 인증서와 관리자 계정 비밀번호를 입력합니다.

+

[NOTE]
====
관리자 계정 비밀번호는 5자 이상 48자 미만이어야 합니다.
====

+

. btn:[Enter] 키를 누릅니다.

. 관리 계정의 이메일 주소를 입력합니다.  \btn:[Enter]를 누릅니다.

+

btn:[Enter] 키를 누릅니다.

. 배포가 완료될 때까지 기다립니다.

. 브라우저를 열고 서버 FQDN으로 이동합니다.

. 사용자 이름(기본값은 `admin`)과 배포 프로세스 중에 설정한 비밀번호를 입력합니다.

이 가이드에서는 {productname} {productnumber} 서버를 컨테이너로 배포했습니다.
다음 섹션으로 진행하여 {scclongform}와 동기화를 위한 조직 자격 증명을 추가하십시오.


== {productname} {productnumber}를 {scclongform}에 연결합니다.

이 섹션에서는 {scc}을 {webui}에서 동기화하고 첫 번째 클라이언트 채널을 추가하는 방법에 대해 설명합니다.

[[proc-admin-organization-credentials]]
.절차 조직 자격 증명 입력

. 브라우저를 열고 서버 FQDN으로 이동합니다.

. 사용자 이름(기본값은 `admin`)과 배포 프로세스 중에 설정한 비밀번호를 입력합니다.

. {productname} {webui}에서 menu:관리자[설정 마법사]를 선택합니다.

. `Setup Wizard` 페이지에서 btn:[조직 자격 증명] 탭을 선택합니다.

. btn:[새 인증서 추가]를 클릭합니다.

. 브라우저를 {scclongform}로 가리킵니다.

. 왼쪽 탐색에서 조직을 선택합니다.

. 페이지 상단에서 사용자 탭을 선택한 다음 btn:[조직 자격 증명]을 선택합니다.

. 귀하의 **미러링 자격 증명**을 기록해 둡니다.

. {productname} {webui}로 돌아가 `Username`와 `Password`를 입력하고 btn:[저장]을 눌러 확인합니다.

자격 증명이 초록색 체크 마크 아이콘으로 확인되면 <<proc-quickstart-first-channel-sync>>을 진행합니다.


[[proc-quickstart-first-channel-sync]]
.절차 {scclongform}와 동기화 중입니다.

. {webui}에서 menu:관리자[설정 마법사]로 이동합니다.

. `Setup Wizard` 페이지에서 `SUSE Products` 탭을 선택합니다.

+

최근에 {scclongform}에 등록한 경우 제품 목록이 테이블에 채워지기 시작합니다.
이 작업은 몇 분 정도 걸릴 수 있습니다.

+

오른쪽의 `Refresh the product catalog from {scclongform}` 섹션에서 작업 진행 상황을 모니터링할 수 있습니다.

+

제품 테이블에는 아키텍처, 채널 및 상태 정보가 나열됩니다.
자세한 내용은 xref:reference:admin/setup-wizard.adoc[마법사]를 참조하십시오.

+

image::admin_suse_products.png[scaledwidth=80%]

+

. `Filter by product description` 및 `Filter by architecture`를 사용하여 표시된 제품 목록을 필터링합니다.

+

btn:[제품] 페이지에 나열된 채널은 클라이언트를 위한 리포지토리를 제공합니다.

+

* 각 채널의 왼쪽에 있는 확인란을 선택하여 {productname}에 채널을 추가합니다.

+

설명의 왼쪽에 있는 화살표 기호를 클릭하여 제품을 펼치고 사용할 수 있는 모듈을 나열합니다.

+

* 페이지 상단의 btn:[제품 추가]를 클릭하여 제품 동기화를 시작합니다.

채널을 추가한 후, {productname}이(가) 채널 동기화를 예약합니다.
이 과정은 {productname}가 {scclongform}에 위치한 {suse} 저장소에서 채널 소프트웨어 소스를 복사하여 서버의 로컬 `/var/lib/containers/storage/volumes/var-spacewalk/` 디렉토리에 저장하기 때문에 오랜 시간이 걸릴 수 있습니다.

채널이 완전히 동기화되면, 이를 위한 부팅 리포지토리가 자동으로 생성됩니다.
이 단계는 클라이언트를 성공적으로 부팅하기 위해 매우 중요하며, 클라이언트 측에서 채널 동기화 및 배포가 작동하도록 보장합니다. 이로써 {productname}의 설치 및 구성이 완료되며, 클라이언트를 부팅하는 데 필요한 채널이 준비됩니다.

채널 동기화 프로세스가 완료되면 {productname} {productnumber} 프록시 또는 추가 클라이언트를 등록할 수 있습니다.

자세한 지침은 xref:client-configuration:registration-overview.adoc[]을 참조하십시오.



== 관리용 컨테이너 입력

컨테이너 내부의 셸로 이동하려면 컨테이너 호스트에서 실행합니다.
----
mgrctl term
----
