[NOTE]
====
보안이 강화된 {productname} 서버 호스트는 `/tmp` 폴더의 파일 실행을 제한할 수 있습니다.
이러한 경우, 해결 방법으로 `mgradm`를 실행하기 전에 `TMPDIR` 환경 변수를 다른 기존 경로로 내보내십시오.

예:

[source,shell]
----
export TMPDIR=/path/to/other/tmp
----

{productname} 업데이트에서는 이 해결 방법이 불필요하도록 도구가 변경될 예정입니다.
====
