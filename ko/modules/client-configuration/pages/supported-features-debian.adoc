[[supported-features-debian]]
= 지원되는 {debian} 기능
:description: Debian 클라이언트에서 지원되는 기능에 대해 알아보세요. 여기에는 시스템 패키지 관리, 등록, 패키지 설치, 패치 적용, 원격 기능이 포함됩니다.
:revdate: 2025-03-10
:page-revdate: {revdate}
:page-author: SUSE 제품 및 솔루션 문서 팀
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

아래 표는 {debian} 클라이언트에서 다양한 기능의 사용 가능 여부를 정리한 것입니다.

[IMPORTANT]
====
클라이언트에서 실행하는 운영 체제는 운영 체제를 제공하는 조직이 지원합니다. {debian}는 {debian} 커뮤니티에 의해 지원됩니다.
====

이 표의 아이콘에는 다음과 같은 의미가 있습니다.

* {check} 기능을 사용할 수 있습니다.
* {cross}이 기능을 사용할 수 없습니다.
* {question} 기능은 현재 검토 중이며, 향후 사용 여부가 결정될 수 있습니다.


[cols="1,1,1", options="header"]
.운영 체제에서 {debian} 지원되는 기능
|===

| 기능
| {debian}{nbsp}12
| {debian}{nbsp}13

| 클라이언트
| {check}
| {check}

| 시스템 패키지
| {debian} 커뮤니티
| {debian} 커뮤니티

| 등록
| {check}
| {check}

| 패키지 설치
| {check}
| {check}

| 패치 적용
| {question}
| {question}

| 원격 명령
| {check}
| {check}

| 시스템 패키지 상태
| {check}
| {check}

| 시스템 사용자 정의 상태
| {check}
| {check}

| 그룹 사용자 정의 상태
| {check}
| {check}

| 조직 사용자 정의 상태
| {check}
| {check}

| 시스템 설정 관리자 (SSM)
| {check}
| {check}

| 제품 마이그레이션
| N/A
| N/A

| 기본 가상 게스트 관리 {star}
| {check}
| {check}

| 고급 가상 게스트 관리 {star}
| {check}
| {check}

| 호스트 OS로서의 가상 게스트 설치 (Kickstart)
| {check}
| {check}

| 호스트 OS로서의 가상 게스트 설치 (이미지 템플릿)
| {check}
| {check}

| 시스템 배포 (PXE/Kickstart)
| {check}
| {check}

| 시스템 재배포 (Kickstart)
| {cross}
| {cross}

| 연락 방법
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH

| {productname} 프록시와 함께 작동
| {check}
| {check}

| 동작 체인
| {check}
| {check}

| 스테이징 (패키지 사전 다운로드)
| {check}
| {check}

| 중복 패키지 보고
| {check}
| {check}

| CVE 감사
| {question}
| {question}

| SCAP 감사
| {question}
| {question}

| 패키지 확인
| {cross}
| {cross}

| 패키지 잠금
| {check}
| {check}

| 시스템 잠금
| {cross}
| {cross}

| 유지보수 기간
| {check}
| {check}

| 시스템 스냅샷
| {cross}
| {cross}

| 구성 파일 관리
| {check}
| {check}

| 패키지 프로파일
| {check} 프로파일 지원, 동기화 미지원
| {check} 프로파일 지원, 동기화 미지원

| 전원 관리
| {check}
| {check}

| 모니터링 서버
| {cross}
| {cross}

| 클라이언트 모니터링
| {check}
| {check}

| Docker 빌드호스트
| {question}
| {question}

| OS를 사용하여 Docker 이미지를 빌드하다
| {salt}
| {salt}

| 키위 빌드호스트
| {cross}
| {cross}

| OS를 사용하여 키위 이미지를 빌드하다
| {cross}
| {cross}

| 반복 작업
| {check}
| {check}

| AppStreams
| N/A
| N/A

| 요미
| N/A
| N/A

|===

{star} 가상 게스트 관리:

이 표에서 가상 게스트 관리는 기본과 고급으로 구분되어 있습니다.

기본 가상 게스트 관리에는 VM, 느린 새로 고침, VM 라이프싸이클 작업(시작, 중지, 다시 시작, 일시 중지) 나열과 VM vCPU 및 메모리 수정이 포함됩니다.

고급 가상 게스트 관리에는 기본 가상 게스트 관리의 모든 기능과 함께 빠른 새로 고침, VM 라이프싸이클 작업(삭제, 재설정, 전원 끄기), VM 디스크, 네트워크, 그래픽 표시 및 그래픽 표시 구성 수정이 포함됩니다.
