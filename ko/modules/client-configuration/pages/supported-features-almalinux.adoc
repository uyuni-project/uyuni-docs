[[supported-features-almalinux]]
= 지원되는 {almalinux} 기능
:description: 지원되는 AlmaLinux 기능의 중앙 집중식 목록에 액세스하여 다양한 클라이언트에서의 사용 가능성을 이해합니다.
:revdate: 2024-09-30
:page-revdate: {revdate}
:page-author: SUSE 제품 및 솔루션 문서팀
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1


이 표는 {almalinux} 클라이언트에서 다양한 기능의 사용 가능 여부를 정리한 것입니다.


[IMPORTANT]
====
클라이언트에서 실행하는 운영 체제는 운영 체제를 제공하는 조직이 지원합니다. {almalinux}은 {almalinux} 커뮤니티에서 지원됩니다.
====



이 표의 아이콘에는 다음과 같은 의미가 있습니다.

* {check} 이 기능은 사용 가능합니다.
* {cross} 이 기능은 사용할 수 없습니다.
* {question} 이 기능은 현재 고려 중이며, 이후 사용 가능 여부가 결정될 수 있습니다.


[cols="1,1,1,1", options="header"]
.{almalinux} 운영 체제에서 지원되는 기능
|===

| 기능
| {almalinux}{nbsp}8
| {almalinux}{nbsp}9
| {almalinux}{nbsp}10

| 클라이언트
| {check} (일반 {almalinux})
| {check} (일반 {almalinux})
| {check} (일반 {almalinux})

| 시스템 패키지
| {almalinux} 커뮤니티
| {almalinux} 커뮤니티
| {almalinux} 커뮤니티

| 등록
| {check}
| {check}
| {check}

| 패키지 설치
| {check}
| {check}
| {check}

| 패치 적용
| {check}
| {check}
| {check}

| 원격 명령
| {check}
| {check}
| {check}

| 시스템 패키지 상태
| {check}
| {check}
| {check}

| 시스템 사용자 정의 상태
| {check}
| {check}
| {check}

| 그룹 사용자 정의 상태
| {check}
| {check}
| {check}

| 조직 사용자 정의 상태
| {check}
| {check}
| {check}

| 시스템 설정 관리자 (SSM)
| {check}
| {check}
| {check}

| 제품 마이그레이션 {star}
| {check}
| {check}
| {check}

| 시스템 배포(PXE/Kickstart)
| {check}
| {check}
| {check}

| 시스템 재배포 (Kickstart)
| {check}
| {check}
| {check}

| 연락 방법
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH
| {check} ZeroMQ, Salt-SSH

| {productname}프록시와 함께 작동
| {check}
| {check}
| {check}

| 작업 체인
| {check}
| {check}
| {check}

| 스테이징 (패키지 사전 다운로드)
| {check}
| {check}
| {check}

| 중복 패키지 보고
| {check}
| {check}
| {check}

| CVE 감사
| {check}
| {check}
| {check}

| SCAP 감사
| {check}
| {check}
| {check}

| 패키지 검증
| {cross}
| {cross}
| {cross}

| 패키지 잠금
| {check}
| {check}
| {check}

| 시스템 잠금
| {cross}
| {cross}
| {cross}

| 유지보수 기간
| {check}
| {check}
| {check}

| 시스템 스냅샷
| {cross}
| {cross}
| {cross}

| 구성 파일 관리
| {check}
| {check}
| {check}

| 스냅샷 및 프로파일
| {check} 지원되는 프로파일, 동기화 미지원
| {check} 프로파일 지원, 동기화 미지원
| {check} 지원되는 프로파일, 동기화 미지원

| 전원 관리
| {check}
| {check}
| {check}

| 모니터링 서버
| {cross}
| {cross}
| {cross}

| 모니터링된 클라이언트
| {check}
| {check}
| {check}

| Docker 빌드호스트
| {cross}
| {cross}
| {cross}

| OS로 Docker 이미지 빌드
| {cross}
| {cross}
| {cross}

| Kiwi 빌드호스트
| {cross}
| {cross}
| {cross}

| OS로 Kiwi 이미지 빌드
| {cross}
| {cross}
| {cross}

| 반복 작업
| {check}
| {check}
| {check}

| AppStreams
| {check}
| {check}
| {check}

| 요미
| N/A
| N/A
| N/A

|===


{star} 제품을 일치하는 {sll} 버전으로 마이그레이션합니다.
