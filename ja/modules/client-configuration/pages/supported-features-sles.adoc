[[supported-features-sles]]
= {slsa}および{opensuse}のサポートされている機能
:description: SUSEおよびopenSUSEクライアントの機能の可用性を、異なるオペレーティングシステムのバリアント間で比較します
:revdate: 2024-05-10
:page-revdate: {revdate}
:page-author: SUSE製品およびソリューションドキュメンテーションチーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1


この表には、{suse}および{opensuse}クライアントのさまざまな機能の使用可否がリストされています。
この表は、{sle}SAP用の{slsa}、{sleda}、{sles}、およびHPC用の{sles}を含むオペレーティングシステムのすべてのバリアントをカバーしています。

[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。

* {sle}は{suse}によってサポートされています。
** {suse}は、一般サポートまたは長期サポート（LTS）の対象となる製品のみをサポートしています。
* {opensuse}は{suse}コミュニティによってサポートされています。

====

この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用可能です
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です


[cols="1,1,1,1,1,1", options="header"]
.{suse}および{opensuse}オペレーティングシステムでサポートされている機能
|===

| 機能
| {sle}{nbsp}12
| {sle}{nbsp}15
| {sle}{nbsp}16
| {leap} 15
| {leap} 16

| [クライアント]
| {check}
| {check}
| {check}
| {check}
| {check}

| システムパッケージ
| {suse}
| {suse}
| {suse}
| {opensuse}コミュニティ
| {opensuse}コミュニティ

| 登録
| {check}
| {check}
| {check}
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}
| {check}
| {check}
| {check}

| パッチの適用
| {check}
| {check}
| {check}
| {check}
| {check}

| リモートコマンド
| {check}
| {check}
| {check}
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}
| {check}
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}
| {check}
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}
| {check}
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}
| {check}
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}
| {check}
| {check}
| {check}

| 製品の移行
| {check}
| {check}
| {check} (*)
| {check}
| {question}

| システムデプロイメント（PXE/{ay}）
| {check}
| {check}
| {question}
| {check}
| {question}

| システム再デプロイメント（{ay}）
| {check}
| {check}
| {question}
| {cross}
| {question}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ, Salt-SSH

| アクションチェーン
| {check}
| {check}
| {check}
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}
| {check}
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}
| {check}
| {check}
| {check}

| CVE監査
| {check}
| {check}
| {check}
| {check}
| {check}

| SCAP監査
| {check}
| {check}
| {question}
| {check}
| {question}

| パッケージロック
| {check}
| {check}
| {check}
| {check}
| {check}

| システムのロック
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

| メンテナンスウィンドウ
| {check}
| {check}
| {check}
| {check}
| {check}

| システムスナップショット
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}
| {check}
| {check}
| {check}

| パッケージプロファイル
| {check} プロファイルがサポートされていますが、syncはサポートされていません
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。

| 電源管理
| {check}
| {check}
| {check}
| {check}
| {check}

| モニタリングサーバ
| {cross}
| {check}
| {question}
| {check}
| {question}

| 監視対象のクライアント
| {check}
| {check}
| {question}
| {check}
| {question}

| Dockerビルドホスト
| {check}
| {check}
| {question}
| {question}
| {question}

| OSを使用してDockerイメージをビルド
| {check}
| {check}
| {check}
| {check}
| {check}

| Kiwiビルドホスト
| {check}
| {question}
| {question}
| {question}
| {question}

| OSを使用してKiwiイメージをビルド
| {check}
| {question}
| {question}
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}
| {check}
| {check}
| {check}

| AppStream
| N/A
| N/A
| N/A
| N/A
| N/A

| 読み
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}

|===



[NOTE]
====
(*) {sles} 16.0への移行は、{sles} 15 SP7からのみサポートされています。

移行は現在、通常のミニオンに対してのみサポートされています。Salt SSHで管理されているミニオンについては、移行はまだサポートされていません。
====
