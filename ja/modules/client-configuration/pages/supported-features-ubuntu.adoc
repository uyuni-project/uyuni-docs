[[supported-features-ubuntu]]
= サポートされている{ubuntu}機能
:description: CanonicalがUbuntuクライアント向けにサポートしているさまざまな機能について学びます。これには、登録、パッケージのインストール、リモートコマンド、システムパッケージが含まれます。
:revdate: 2025-03-13
:page-revdate: {revdate}
:page-author: SUSE製品およびソリューションドキュメンテーションチーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1


この表には、{ubuntu}クライアントのさまざまな機能の使用可否がリストされています。

[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。{ubuntu}はCanonicalでサポートされています。
====

この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用できます
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です

ifeval::[{mlm-content} == true]


[cols="1,1,1", options="header"]
.{ubuntu}オペレーティングシステムでサポートされている機能
|===

| 機能
| {ubuntu}{nbsp}22.04
| {ubuntu}{nbsp}24.04

| [クライアント]
| {check}
| {check}

| システムパッケージ
| {ubuntu}コミュニティ
| {ubuntu}コミュニティ

| 登録
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}

| パッチの適用
| {check}
| {check}

| リモートコマンド
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}

| 製品の移行
| N/A
| N/A

| システムデプロイメント（PXE/Kickstart）
| {cross}
| {cross}

| システム再デプロイメント（Kickstart）
| {cross}
| {cross}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}
| {check}

| アクションチェーン
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}

| CVE監査
| {check}
| {check}

| SCAP監査
| {question}
| {question}

| パッケージ検証
| {cross}
| {cross}

| パッケージのロック
| {check}
| {check}

| メンテナンスウィンドウ
| {check}
| {check}

| システムのロック
| {cross}
| {cross}

| システムスナップショット
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}

| パッケージプロファイル
| {check} プロファイルはサポートされていますが、syncはサポートされていません
| {check} プロファイルはサポートされていますが、syncはサポートされていません

| 電源管理
| {check}
| {check}

| モニタリングサーバ
| {cross}
| {cross}

| 監視対象のクライアント
| {check}
| {check}

| Dockerビルドホスト
| {question}
| {question}

| OSを使用してDockerイメージをビルド
| {check}
| {check}

| Kiwiビルドホスト
| {cross}
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}

| AppStream
| N/A
| N/A

| 読み
| N/A
| N/A

|===

endif::[]


ifeval::[{uyuni-content} == true]


[cols="1,1,1,1", options="header"]
.{ubuntu}オペレーティングシステムでサポートされている機能
|===

| 機能
| {ubuntu}{nbsp}20.04
| {ubuntu}{nbsp}22.04
| {ubuntu}{nbsp}24.04

| [クライアント]
| {check}
| {check}
| {check}

| システムパッケージ
| Canonical
| Canonical
| Canonical

| 登録
| {check}
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}
| {check}

| パッチの適用
| {check}
| {check}
| {check}

| リモートコマンド
| {check}
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}
| {check}

| 製品の移行
| N/A
| N/A
| N/A

| システムデプロイメント（PXE/Kickstart）
| {cross}
| {cross}
| {cross}

| システム再デプロイメント（Kickstart）
| {cross}
| {cross}
| {cross}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}
| {check}
| {check}

| アクションチェーン
| {check}
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}
| {check}

| CVE監査
| {check}
| {check}
| {check}

| SCAP監査
| {question}
| {question}
| {question}

| パッケージ検証
| {cross}
| {cross}
| {cross}

| パッケージのロック
| {check}
| {check}
| {check}

| システムのロック
| {cross}
| {cross}
| {cross}

| システムスナップショット
| {cross}
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}
| {check}

| パッケージプロファイル
| {check} プロファイルはサポートされていますが、syncはサポートされていません
| {check} プロファイルはサポートされていますが、syncはサポートされていません
| {check} プロファイルはサポートされていますが、syncはサポートされていません

| 電源管理
| {check}
| {check}
| {check}

| モニタリング
| {check}
| {check}
| {check}

| Dockerビルドホスト
| {question}
| {question}
| {question}

| OSを使用してDockerイメージをビルド
| {check}
| {check}
| {check}

| Kiwiビルドホスト
| {cross}
| {cross}
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}
| {check}

| AppStream
| N/A
| N/A
| N/A

| 読み
| N/A
| N/A
| N/A

|===

endif::[]
