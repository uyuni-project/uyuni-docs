[[supported-features-debian]]
= サポートされている {debian} 機能
:description: Debianクライアントがサポートする機能について学びます。これには、システムパッケージ管理、登録、パッケージのインストール、パッチの適用、リモートが含まれます。
:revdate: 2025-03-10
:page-revdate: {revdate}
:page-author: SUSE製品およびソリューションドキュメンテーションチーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

この表には、{debian}クライアントにおける各種機能の利用可能性が示されています。

[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。{debian}は{debian}コミュニティでサポートされています。
====

この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用できます
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です


[cols="1,1,1", options="header"]
.{debian}オペレーティングシステムでサポートされている機能
|===

| 機能
| {debian}{nbsp}12
| {debian}{nbsp}13

| [クライアント]
| {check}
| {check}

| システムパッケージ
| {debian}コミュニティ
| {debian}コミュニティ

| 登録
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}

| パッチの適用
| {question}
| {question}

| リモートコマンド
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}

| 製品の移行
| N/A
| N/A

| 基本的な仮想ゲスト管理 {star}
| {check}
| {check}

| 高度な仮想ゲスト管理 {star}
| {check}
| {check}

| 仮想ゲストのインストール（Kickstart）、ホストOSとして
| {check}
| {check}

| 仮想ゲストのインストール（イメージテンプレート）、ホストOSとして
| {check}
| {check}

| システムデプロイメント（PXE/Kickstart）
| {check}
| {check}

| システム再デプロイメント（Kickstart）
| {cross}
| {cross}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}
| {check}

| アクションチェーン
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}

| CVE監査
| {question}
| {question}

| SCAP監査
| {question}
| {question}

| パッケージ検証
| {cross}
| {cross}

| パッケージのロック
| {check}
| {check}

| システムのロック
| {cross}
| {cross}

| メンテナンスウィンドウ
| {check}
| {check}

| システムスナップショット
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}

| パッケージプロファイル
| {check} プロファイルがサポートされており、syncはサポートされていません
| {check} プロファイルがサポートされており、syncはサポートされていません

| 電源管理
| {check}
| {check}

| モニタリングサーバ
| {cross}
| {cross}

| クライアントの監視
| {check}
| {check}

| Dockerビルドホスト
| {question}
| {question}

| OSを使用してDockerイメージをビルド
| {salt}
| {salt}

| Kiwiビルドホスト
| {cross}
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}

| AppStream
| N/A
| N/A

| 読み
| N/A
| N/A

|===

{star}仮想ゲスト管理:

この表では、仮想ゲスト管理は基本と高度に分割されています。

基本的な仮想ゲスト管理には、VMのリスト化、低速更新、VMのライフサイクルアクション(起動、停止、再開、一時停止)、およびVM vCPUとメモリの変更が含まれています。

高度な仮想ゲスト管理には、基本的な仮想ゲスト管理のすべての機能に加えて、高速更新、VMライフサイクルアクション(削除、リセット、電源オフ)、VMディスクの変更、ネットワークグラフィカル表示の変更、およびグラフィカル表示の設定が含まれています。
