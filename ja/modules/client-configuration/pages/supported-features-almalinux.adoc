[[supported-features-almalinux]]
= サポートされている {almalinux} 機能
:description: サポートされているAlmaLinux機能の一元化された一覧にアクセスし、さまざまなクライアントでの利用可能性を確認できます。
:revdate: 2024-09-30
:page-revdate: {revdate}
:page-author: SUSE 製品＆ソリューション ドキュメンテーション チーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1


この表には、{almalinux}クライアントにおける各種機能の利用可能性が示されています。


[IMPORTANT]
====
クライアントで実行しているオペレーティングシステムは、オペレーティングシステムを提供している組織によってサポートされています。{almalinux}は{almalinux}コミュニティでサポートされています。
====



この表のアイコンの意味は次のとおりです。

* {check}: 機能は使用可能です
* {cross}: 機能は使用できません
* {question}: 機能は検討中であり、後日利用可能になるかどうかは未定です


[cols="1,1,1,1", options="header"]
.オペレーティングシステム{almalinux}でサポートされている機能
|===

| 機能
| {almalinux}{nbsp}8
| {almalinux}{nbsp}9
| {almalinux}{nbsp}10

| [クライアント]
| {check}（プレーン {almalinux}）
| {check}（プレーン {almalinux}）
| {check}（プレーン {almalinux}）

| システムパッケージ
| {almalinux}コミュニティ
| {almalinux}コミュニティ
| {almalinux}コミュニティ

| 登録
| {check}
| {check}
| {check}

| パッケージのインストール
| {check}
| {check}
| {check}

| パッチの適用
| {check}
| {check}
| {check}

| リモートコマンド
| {check}
| {check}
| {check}

| システムパッケージの状態
| {check}
| {check}
| {check}

| システムカスタム状態
| {check}
| {check}
| {check}

| グループカスタム状態
| {check}
| {check}
| {check}

| 組織カスタム状態
| {check}
| {check}
| {check}

| システムセットマネージャ（SSM）
| {check}
| {check}
| {check}

| 製品の移行 {star}
| {check}
| {check}
| {check}

| システムデプロイメント（PXE/Kickstart）
| {check}
| {check}
| {check}

| システム再デプロイメント（Kickstart）
| {check}
| {check}
| {check}

| 連絡方法
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH
| {check} ZeroMQ、Salt-SSH

| {productname} プロキシと連携
| {check}
| {check}
| {check}

| アクションチェーン
| {check}
| {check}
| {check}

| ステージング（パッケージの事前ダウンロード）
| {check}
| {check}
| {check}

| 重複パッケージの報告
| {check}
| {check}
| {check}

| CVE監査
| {check}
| {check}
| {check}

| SCAP監査
| {check}
| {check}
| {check}

| パッケージ検証
| {cross}
| {cross}
| {cross}

| パッケージのロック
| {check}
| {check}
| {check}

| システムのロック
| {cross}
| {cross}
| {cross}

| メンテナンスウィンドウ
| {check}
| {check}
| {check}

| システムスナップショット
| {cross}
| {cross}
| {cross}

| 設定ファイル管理
| {check}
| {check}
| {check}

| スナップショットとプロファイル
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。
| {check} プロファイルはサポートされていますが、syncはサポートされていません。

| 電源管理
| {check}
| {check}
| {check}

| モニタリングサーバ
| {cross}
| {cross}
| {cross}

| 監視対象のクライアント
| {check}
| {check}
| {check}

| Dockerビルドホスト
| {cross}
| {cross}
| {cross}

| OSを使用してDockerイメージをビルド
| {cross}
| {cross}
| {cross}

| Kiwiビルドホスト
| {cross}
| {cross}
| {cross}

| OSを使用してKiwiイメージをビルド
| {cross}
| {cross}
| {cross}

| 定期的なアクション
| {check}
| {check}
| {check}

| AppStream
| {check}
| {check}
| {check}

| 読み
| N/A
| N/A
| N/A

|===


{star}の製品移行は、対応する{sll}バージョンへの移行となります。
