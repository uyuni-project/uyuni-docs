[[proxy-install-vm]]
= {productname}仮想マシンとしてのプロキシのデプロイメント - KVM
:description: SUSE Multi-Linux Manager 5.1プロキシのデプロイメントを、KVMとVirtual Machine Manager（virt-manager）を使用して仮想マシンとして構成し、成功するように設定します。
:revdate: 2025-06-26
:page-revdate: {revdate}
:page-author: SUSE製品およびソリューションドキュメンテーションチーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1
ifeval::[{uyuni-content} == true]

:noindex:
endif::[]

この章では、{productname} {productnumber}プロキシのイメージとしてのデプロイメントに関する仮想マシンの設定を提供します。
KVMは、このインストールのためのサンドボックスとして、Virtual Machine Manager（_virt-manager_）と組み合わされます。



// FIXME: make this section a snippet or move it to a general intro for both,
//        KVM and VMware
== 利用可能なイメージ


[IMPORTANT]
====
{productname}プロキシをデプロイするための推奨方法は、以下の利用可能なイメージのいずれかを使用することです。
すべてのツールはこれらのイメージに含まれており、デプロイメントが簡素化されています。
====

// 2025-02-17, ke: as of today, suse-manager directory is still in use
{productname} {productnumber}プロキシのイメージは、link:https://www.suse.com/download/suse-manager/[{productname} {productnumber} VMイメージ]で入手可能です。

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-warn-images-sl-micro.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-warn-images-sl-micro.adoc[]
endif::[]

[NOTE]
====
生イメージの準備に関する詳細については、 https://documentation.suse.com/sle-micro/6.1/html/Micro-deployment-raw-images-virtual-machines/index.html#deployment-preparing-configuration-device[]を参照してください。

セルフインストールイメージに関する追加情報については、 https://documentation.suse.com/sle-micro/6.1/html/Micro-deployment-selfinstall-images/index.htmlを参照してください。
====

// FIXME more archs supported?

.利用可能なプロキシイメージ
[cols="3, 3", options="header"]
|===
| アーキテクチャ | イメージフォーマット

| aarch64| qcow2、vmdk

| x86_64  | qcow2、vmdk、raw、セルフインストーラー

|===


[[proxy-quickstart.sect.kvm.settings]]
== 仮想マシンマネージャ(virt-manager)の設定

*virt-manager*を使用して新しい仮想マシンを作成する際に、次の設定を入力してください。

[NOTE]
====
この表では、最低要件を指定しています。
これらは、クライアント1台のプロキシなど、迅速なテストインストールに適しています。

本番環境を使用したい場合や、ディスクスペースに関する背景情報が必要な場合は、xref:installation-and-upgrade:hardware-requirements.adoc[]を参照してください。
====


[cols="1,1", options="header"]
|===
2+<| KVMの設定
| インストール方法 | 既存のディスクイメージをインポート
| OS:                 | Linux
| バージョン:            | {productname}-Proxy.x86_64-5.1.*.qcow2
| メモリ:             | 最小 *)
| CPU：              | 最小 *)
| ストレージフォーマット:    | _.qcow2_ 40 GB (デフォルト) ルートパーティション
| 名前:               | test-setup
| ネットワーク             | ブリッジ _br0_
|===

*) 最小値については、xref:installation-and-upgrade:hardware-requirements.adoc#proxy-hardware-requirements[]を参照してください。


[NOTE]
====
`/var/lib/containers/storage/volumes` +
最低100 GB。
ストレージ要件は、使用するISOディストリビューションイメージ、コンテナ、およびブートストラップリポジトリの数に基づいて計算する必要があります。
====



[[proxy-minimmal.kvm.setup]]
== KVMの初期セットアップ

設定については、xref:installation-and-upgrade:container-deployment/mlm/proxy-deployment-vm-mlm.adoc#proxy-quickstart.sect.kvm.settings[]を参照してください。

.プロシージャ:初期セットアップの作成
. ダウンロードした {minimal} KVMイメージを使用して新しい仮想マシンを作成し、`Import existing disk image` を選択します。
. RAMおよびCPU数を最小値で設定します。*)
. KVMマシンに名前を付け、`Customize configuration before install` チェックボックスを選択します。
. btn:[インストールの開始] をクリックして、イメージからブートします。
. ［JeOS Firstboot］の画面で、［Start］を選択して続行します。
+

image::jeos-firstboot.png[]

. キーボードレイアウトを選択します。
+

image::select-keyboard.png[]

.  ライセンス契約に同意します。
+

image::accept-license-agreement.png[]

. タイムゾーンを選択します。
+

image::enter-timezone.png[]

. rootのパスワードを入力します。
+

image::root-password.png[]

. インストールが完了したら、rootとしてログインします。

. 次のセクションに進みます。

*) 最小値については、xref:installation-and-upgrade:hardware-requirements.adoc#proxy-hardware-requirements[]を参照してください。


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-register-proxy-mlm.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-register-proxy-mlm.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-actkey-bootstrap-proxy-mlm.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-actkey-bootstrap-proxy-mlm.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-generate_proxy_config.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-generate_proxy_config.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-transfer_proxy_config.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-transfer_proxy_config.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-start-proxy.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-start-proxy.adoc[]
endif::[]


////
[[installation-proxy-containers-services]]
== Install container services on the host system
ifeval::[{mlm-content} == true]

[NOTE]
====
`Containers Module` is required to be available for container host on a {sles} machine.
====
endif::[]

{productname} Proxy containers are using `podman` and `systemd` to run and manage all proxy containers.

First step is to install the `mgrpxy` utility provided by the package of the same name.

ifeval::[{mlm-content} == true]
The `mgrpxy` package is available in the {productname} Proxy product repositories.
endif::[]

ifeval::[{uyuni-content} == true]
The `mgrpxy` package is available in the container utils repository: pick the one matching the distribution in: https://download.opensuse.org/repositories/systemsmanagement:Uyuni:/Stable:/ContainerUtils/.
endif::[]

To install them run:

----
zypper in helm mgrpxy
----
////


=== サービスにカスタムコンテナイメージを使用する

デフォルトでは、{productname}プロキシスイートは、各サービスに対して同じイメージバージョンとレジストリパスを使用するように設定されています。
ただし、`-tag` および `-image` で終わるインストールパラメータを使用することで、特定のサービスのデフォルト値を上書きすることが可能です。

たとえば、次のように使用します。

----
mgrpxy install podman --httpd-tag 0.1.0 --httpd-image registry.opensuse.org/uyuni/proxy-httpd /path/to/config.tar.gz
----

httpdサービスの設定ファイルを調整し（`registry.opensuse.org/uyuni/proxy-httpds` は使用するイメージ、`0.1.0` はバージョンタグ）、その後再起動します。

値をデフォルトにリセットするには、これらのパラメータを指定せずにもう一度installコマンドを実行します。

----
mgrpxy install podman /path/to/config.tar.gz
----

このコマンドは、すべてのサービスの設定をグローバルデフォルトにリセットして再ロードします。
