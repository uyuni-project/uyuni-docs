= YAMLのカスタム設定と[command]\``mgradm``を使用した配備
:description: 特定の例の形式でカスタムmgradm.yamlファイルを作成する方法を学び、その後、パスワードを保護しながらデプロイメント時にmgradmツールと共に使用します。
:revdate: 2024-11-19
:page-revdate: {revdate}
:page-author: SUSE製品およびソリューションドキュメントチーム
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

カスタムの`mgradm.yaml`ファイルを作成するオプションがあり、`mgradm`ツールはデプロイメント時にこのファイルを利用できます。
すべての`mgradm`引数には、対応するYAML項目が存在します。
例えば、`mgradm`引数`--ssl-db-ca-intermediate`は、次のように`mgradm.yaml`ファイルで指定できます。

[source, yaml]
ssl:
  db:
    ca:
      intermediate: /path/to/ca-intermediate.crt

[IMPORTANT]
====
コマンドライン引数または`mgradm`設定ファイルで基本的な変数が指定されていない場合、`mgradm.yaml`が入力を求めます。

セキュリティのため、**コマンドラインパラメータを使用してパスワードを指定することは避けるべきです**。
代わりに適切な権限を持つ設定ファイルを使用してください。
====

.プロシージャ:カスタム設定ファイルを使用してPodmanで{productname}コンテナをデプロイする
. 次の例のような`mgradm.yaml`という名前の設定ファイルを準備します。
+
[source, yaml]
....
# Database password. Randomly generated by default
db:
  password: MySuperSecretDBPass

# Password for the CA certificate
ssl:
  password: MySuperSecretSSLPassword

# Your SUSE Customer Center credentials
scc: 
  user: ccUsername
  password: ccPassword

# Organization name
organization: YourOrganization

# Email address sending the notifications
emailFrom: notifications@example.com

# Administrators account details
admin:
  password: MySuperSecretAdminPass
  login: LoginName
  firstName: Admin
  lastName: Admin
  email: email@example.com
....

. ターミナルから、ルートとして次のコマンドを実行します。
  サーバーのFQDNを入力することは任意です。
+
[source, shell]
----
mgradm -c mgradm.yaml install podman <FQDN>
----
+
[IMPORTANT]
====
コンテナはsudoまたはルートとしてデプロイする必要があります。
このステップを省略すると、ターミナルに次のエラーが表示されます。

[source, shell]
----
INF Setting up uyuni network
9:58AM INF Enabling system service
9:58AM FTL Failed to open /etc/systemd/system/uyuni-server.service for writing error="open /etc/systemd/system/uyuni-server.service: permission denied"
----
====

. 配備が完了するまで待ちます。

. ブラウザを開き、FQDNまたはIPアドレスの入力に進みます。

//In this section you learned how to deploy an {productname} {productnumber} Server container using a custom YAML configuration.
