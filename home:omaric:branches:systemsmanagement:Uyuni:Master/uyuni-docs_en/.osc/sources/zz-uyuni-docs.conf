RewriteEngine on
RewriteOptions inherit

<Directory "/usr/share/susemanager/www/htdocs/docs">
  # Prepend "/product/version/" to the paths, if the original path is not available
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  # Rewrite only if the prepended path is available
  RewriteCond %{DOCUMENT_ROOT}/docs/$1/uyuni/$2 -f [OR]
  RewriteCond %{DOCUMENT_ROOT}/docs/$1/uyuni/$2 -d
  RewriteRule ^(.*?)/(.*)$ /docs/$1/uyuni/$2 [R]
</Directory>
