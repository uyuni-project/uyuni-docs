[[clients-ubuntu]]
= 注册 {ubuntu} 客户端
:description: 通过添加所需的软件通道、同步它们并管理受支持的产品来配置与您的 SUSE Multi-Linux Manager 服务器的 Ubuntu 客户端
:revdate: 2025-03-13
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

本节包含有关注册运行 {ubuntu} 操作系统的客户端的信息。

ifeval::[{mlm-content} == true]

// Use list format: repository URLs / packages and metadata / version info
[IMPORTANT]
====
* 软件包和元数据由 Ubuntu 提供，而不是由 {suse} 提供。
* 有关支持的产品，请参见 xref:client-configuration:supported-features-ubuntu.adoc[] 中的版本说明和支持表。
====
endif::[]

ifeval::[{mlm-content} == true]

{productname} 支持使用 {ubuntu} 的 {salt} 22.04 LTS 和 24.04 LTS 客户端。
endif::[]

支持使用引导功能启动 {ubuntu} 客户端并执行初始状态运行，例如设置储存库和执行控制文件更新。
但是，{ubuntu} 上的 root 用户默认是禁用的，因此要使用启动，您需要一个具有 `sudo` 权限的现有用户。

[IMPORTANT]
====
Canonical 不为 {productname} 背书，也不为其提供支持。
====

== 添加软件通道


在将 {ubuntu} 客户端注册到您的 {productname} 服务器之前，您需要添加所需的软件通道，并同步这些通道。

ifndef::backend-pdf[]
include::client-configuration:partial$arch-other-note.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/arch-other-note.adoc[]
endif::[]

ifeval::[{mlm-content} == true]

此过程所需的产品包括：

[[ubuntu-channels-wizard]]
[cols="1,1", options="header"]
.Ubuntu 产品 - WebUI
|===

| 操作系统版本     | 产品名称
| {ubuntu} 24.04 | Ubuntu 24.04
| {ubuntu} 22.04 | Ubuntu 22.04
|===

ifndef::backend-pdf[]
include::client-configuration:partial$addchannels_vendor_webui.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/addchannels_vendor_webui.adoc[]
endif::[]

或者，您也可以在命令提示符处添加通道。
此过程所需的通道包括：

[[ubuntu-channels-cli]]
[cols="1,1", options="header"]
.Ubuntu 通道 - CLI
|===

| 操作系统版本     | 基础通道
| {ubuntu} 24.04 | ubuntu-2404-amd64-main-amd64
| {ubuntu} 22.04 | ubuntu-2204-amd64-main-amd64
|===

ifndef::backend-pdf[]
include::client-configuration:partial$addchannels_vendor_cli.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/addchannels_vendor_cli.adoc[]
endif::[]

endif::[]


ifeval::[{uyuni-content} == true]
此过程所需的通道包括：

[[ubuntu-channels-cli-uyuni]]
[cols="1,1,1,1,1,1", options="header"]
.Ubuntu 通道 - CLI
|===

| 操作系统版本 | 基础通道 | 主通道 | 更新通道 | 安全通道 | 客户端通道

| {ubuntu} 24.04 | ubuntu-2404-pool-amd64-uyuni | ubuntu-2404-amd64-main-uyuni | ubuntu-2404-amd64-main-updates-uyuni | ubuntu-2404-amd64-main-security-uyuni | ubuntu-2404-amd64-uyuni-client
| {ubuntu} 22.04 | ubuntu-2204-pool-amd64-uyuni | ubuntu-2204-amd64-main-uyuni | ubuntu-2204-amd64-main-updates-uyuni | ubuntu-2204-amd64-main-security-uyuni | ubuntu-2204-amd64-uyuni-client
| {ubuntu} 20.04 | ubuntu-2004-pool-amd64-uyuni | ubuntu-2004-amd64-main-uyuni | ubuntu-2004-amd64-main-updates-uyuni | ubuntu-2004-amd64-main-security-uyuni | ubuntu-2004-amd64-uyuni-client
|===

版本 20.04 还需要通用通道：

[[ubuntu-universe-channels-cli-uyuni]]
[cols="1,1", options="header"]
.Ubuntu 20.04 通用通道 - CLI
|===

| {ubuntu} 20.04           | {nbsp}
| 宇宙通道         | ubuntu-2004-amd64-universe-uyuni
| 宇宙更新通道 | ubuntu-2004-amd64-universe-updates-uyuni
| 宇宙安全更新通道 | ubuntu-2004-amd64-universe-security-uyuni
| 宇宙向后移植通道 | ubuntu-2004-amd64-universe-backports-uyuni

|===

ifndef::backend-pdf[]
include::client-configuration:partial$addchannels_novendor_cli.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/addchannels_novendor_cli.adoc[]
endif::[]

[IMPORTANT]
====
在引导任何 Ubuntu 客户端之前，您需要完全同步所有新通道。
====

endif::[]


== 镜像 Ubuntu ESM 软件包

Canonical 为 https://ubuntu.com/pro[Ubuntu Pro] 用户和客户提供 https://ubuntu.com/security/esm[扩展安全维护] (**ESM**) 软件包。
这些软件包为多个操作系统组件和选定应用程序提供更长的维护时间 (_10 到 12 年_)。

如果您从已注册到 Ubuntu Pro 的系统中提取所需的 **GPG 密钥** 和您的个人 **Bearer 词元**，则这些储存库还可以在 {productname} 中同步。


=== 提取 GPG 密钥和持有者令牌

使用 Ubuntu Pro 注册 Ubuntu 主机。
您可以在 https://ubuntu.com/pro/dashboard[Ubuntu Pro Dashboard] 中找到您的个人注册词元。
这需要一个 https://login.ubuntu.com/[Ubuntu One 账户]。

----
sudo apt-get install ubuntu-advantage-tools
sudo pro attach <perosnal_token>
----

注册后，您将在文件 `/etc/apt/auth.conf.d/90ubuntu-advantage` 中找到 Bearer 词元：

----
machine esm.ubuntu.com/apps/ubuntu/ login bearer password <token>  # ubuntu-pro-client
machine esm.ubuntu.com/infra/ubuntu/ login bearer password <token>  # ubuntu-pro-client
----

[NOTE]
====
将为每个储存库使用一个专用的持有者令牌。
====

在 {productname} 中配置以下储存库：

=== 配置 Ubuntu ESM 储存库

使用以下 URL 创建储存库：

[[ubuntu-esm-repositories]]
[cols="1,1", options="header"]
.Ubuntu ESM 储存库
|===

| URL     | 说明
| https://bearer:<token>@esm.ubuntu.com/infra/ubuntu/dists/<release>-infra-updates/main/binary-<arch>/ | 操作系统功能更新
| https://bearer:<token>@esm.ubuntu.com/infra/ubuntu/dists/<release>-infra-security/main/binary-<arch>/ | 操作系统安全更新
| https://bearer:<token>@esm.ubuntu.com/apps/ubuntu/dists/<release>-apps-updates/main/binary-<arch>/ | 应用程序功能更新
| https://bearer:<token>@esm.ubuntu.com/apps/ubuntu/dists/<release>-apps-security/main/binary-<arch>/ | 应用程序安全更新
|===

将 `<token>` 替换为您的个人 Bearer 词元。此外，`arch` 和 `release` 必须替换为以下值之一：

[[ubuntu-esm-archs-releases]]
[cols="1,1", options="header"]
.Ubuntu ESM 体系结构和版本
|===

| 架构 | 版本
| `amd64`、`arm64`、`armel`、`armhf`、`i386`、`powerpc`、`ppc64el`、`s390x`| `bionic`, `focal`, `jammy`, `noble`, `trusty`, `xenial`
|===

为了使 {productname} 同步储存库，必须导入相应的 GPG 密钥 (`ubuntu-advantage-esm-infra-trusty.gpg`, `ubuntu-advantage-esm-apps.gpg`)。这些密钥位于注册了 Ubuntu Pro 的系统上，注册名为 `/etc/apt/trusted.gpg.d`。将这些文件复制到 {productname} 系统并按如下方式导入：

----
mgradm gpg add /path/to/gpg.key
----

在已同步的 {ubuntu} 父通道下创建适当的子通道。之后，可以同步储存库。

[IMPORTANT]
====
此处显示的程序可用于规避订阅限制 - 然而，这构成了违反服务条款，可能会导致 **法律后果**。始终必须有足够的订阅以支持所使用的系统数量。
====

== 检查同步状态

ifeval::[{mlm-content} == true]

ifndef::backend-pdf[]
include::client-configuration:partial$check_sync_webui_mlm.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/check_sync_webui_mlm.adoc[]
endif::[]

endif::[]

ifeval::[{uyuni-content} == true]

ifndef::backend-pdf[]
include::client-configuration:partial$check_sync_webui_uyuni.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/check_sync_webui_uyuni.adoc[]
endif::[]

endif::[]

ifndef::backend-pdf[]
include::client-configuration:partial$check_sync_cli.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/check_sync_cli.adoc[]
endif::[]

[NOTE]
====
{ubuntu} 通道可能非常大。
同步有时可能需要几个小时。
====



== 管理 GPG 密钥

客户端使用 GPG 密钥在安装软件包之前检查其真实性。
只有受信任的软件包才能在客户端上安装。

[IMPORTANT]
====
信任 GPG 密钥对客户端的安全性至关重要。
决定需要哪些密钥并可以信任哪些密钥是管理员的任务。
由于在 GPG 密钥未被信任时无法使用软件通道，因此是否将通道分配给客户端取决于是否信任该密钥。
====

有关 GPG 密钥的更多信息，请参见 xref:client-configuration:gpg-keys.adoc[]。


== Root 访问权限

在 {ubuntu} 上，root 用户默认情况下禁用了 SSH 访问。

要能够使用普通用户进行注册，您需要编辑 `sudoers` 文件。

[NOTE]
====
此问题发生在自安装版本的 {ubuntu} 中。
如果在安装时默认用户被授予了管理权限，则需要输入密码以使用 `sudo` 执行权限提升。
在云实例中不会发生这种情况，因为 `cloud-init` 会自动在 `/etc/sudoers.d` 下创建一个文件，并通过 `sudo` 授予权限提升，而无需密码。
====



=== 授予 Root 用户访问权限

.过程：升级授予 Root 用户访问权限

. 在客户端，编辑 `sudoers` 文件：
+
----
sudo visudo
----
+
在 `sudoers` 文件末尾添加此行，以授予用户 `sudo` 访问权限。
将 `<user>` 替换为在 {webui} 中负责启动客户端的用户名称：
+
----
<user>  ALL=NOPASSWD: /usr/bin/python, /usr/bin/python2, /usr/bin/python3, /var/tmp/venv-salt-minion/bin/python
----

[NOTE]
====
此过程授予 root 访问权限，无需密码，这对于注册客户端是必需的。
当客户端成功安装后，它以 root 权限运行，因此不再需要访问权限。
我们建议在客户端成功安装后，从 `sudoers` 文件中去除此行。
====



=== 通过 SSH 以安装期间创建的用户身份引导

要通过 SSH 引导 Ubuntu 客户端，您必须将安装期间创建的用户添加到 sudo 组。
然后从客户端运行启动脚本。

.过程：升级将安装期间创建的用户添加到 sudo 组并通过 SSH 引导

. 在客户端上，以 root 身份从命令行运行以下命令（将 `<username>` 替换为安装期间创建的用户的名称）：
+

----
sudo usermod -aG sudo <username>
----

. 从命令行启动客户端系统（将 `<SERVER_FQDN>` 替换为 {productname} 服务器的完全限定的域名）：
+

----
sudo su -
curl -Sks https://<SERVER_FQDN>/pub/bootstrap/bootstrap-script.sh | /bin/bash
----

[IMPORTANT]
====
{ubuntu} 只能通过在客户端系统的命令行中运行相应的 {ubuntu} 启动脚本来启动，前提是先执行 `sudo su -`。
由于 {ubuntu} 上的 root 用户默认被禁用，并且 {webui} 不允许 {ubuntu} 安装期间创建的用户进行权限提升，因此无法通过 {productname} {webui} 引导。
====



== 注册客户端

ifndef::backend-pdf[]
include::client-configuration:partial$create_bootstrap_repo_register.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../partials/create_bootstrap_repo_register.adoc[]
endif::[]

