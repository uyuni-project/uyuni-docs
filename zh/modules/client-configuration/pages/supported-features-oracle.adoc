[[supported-features-oracle]]
= 支持的 Oracle 功能
:description: 查看 Oracle Linux 客户端支持的功能，包括系统软件包、注册和补丁管理。
:revdate: 2024-09-30
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

下表列出了 {oracle} 客户端上各种功能的可用性

[IMPORTANT]
====
客户端上运行的操作系统由提供操作系统的组织支持。{oracle} 由 Oracle 支持
====

此表中图标的含意如下：

* {check} 该功能可用
* {cross} 该功能不可用
* {question} 该功能正在考虑之中，日后可能提供，也可能不提供


[cols="1,1,1,1", options="header"]
.操作系统上{oracle}支持的功能
|===
| 功能
| {oracle}{nbsp}8
| {oracle}{nbsp}9
| {oracle}{nbsp}10
| 客户端
| {check}
| {check}
| {check}
| 操作系统包
| {check}
| {check}
| {check}
| 注册
| {check}
| {check}
| {check}
| 安装软件包
| {check}
| {check}
| {check}
| 应用补丁（需要 CVE ID）
| {check}
| {check}
| {check}
| 远程命令
| {check}
| {check}
| {check}
| 系统软件包状态
| {check}
| {check}
| {check}
| 系统自定义状态
| {check}
| {check}
| {check}
| 组自定义状态
| {check}
| {check}
| {check}
| 组织自定义状态
| {check}
| {check}
| {check}
| 系统集管理器（SSM）
| {check}
| {check}
| {check}
| 产品迁移 {star}
| {check}
| {check}
| {check}
| 系统部署（PXE/Kickstart）
| {check}
| {check}
| {check}
| 系统重新部署（Kickstart）
| {check}
| {check}
| {check}
| 联系方式
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| 与 {productname} 代理兼容
| {check}
| {check}
| {check}
| 操作链
| {check}
| {check}
| {check}
| 暂存 (软件包预下载)
| {check}
| {check}
| {check}
| 重复软件包报告
| {check}
| {check}
| {check}
| CVE 审计（需要 CVE ID）
| {check}
| {check}
| {check}
| SCAP 审计
| {check}
| {check}
| {check}
| 软件包锁定
| {check}
| {check}
| {check}
| 系统锁定
| {cross}
| {cross}
| {cross}
| 维护时段
| {check}
| {check}
| {check}
| 系统快照
| {cross}
| {cross}
| {cross}
| 配置文件管理
| {check}
| {check}
| {check}
| 快照和控制文件
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| 电源管理
| {check}
| {check}
| {check}
| 监视服务器
| {cross}
| {cross}
| {cross}
| 监控的客户端
| {check}
| {check}
| {check}
| Docker 构建主机
| {cross}
| {cross}
| {cross}
| 使用操作系统构建 Docker 镜像
| {cross}
| {cross}
| {cross}
| Kiwi 构建主机
| {cross}
| {cross}
| {cross}
| 使用操作系统构建 Kiwi 镜像
| {cross}
| {cross}
| {cross}
| 重复性操作
| {check}
| {check}
| {check}
| AppStream
| {check}
| {check}
| {check}
| Yomi
| 不适用
| 不适用
| 不适用
|===

{star} 产品迁移到匹配的 {sll} 版本。
