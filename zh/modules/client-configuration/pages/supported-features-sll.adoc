[[supported-features-sll]]
= 支持的 {sll} 功能
:description: 查看 SUSE Liberty Linux 客户端上支持的功能的综合列表，包括软件包、注册
:revdate: 2026-04-20
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

下表列出了 {sll} 客户端上各种功能的可用性。

[NOTE]
====
{sll} 客户端有时也称为 {sleses} (SLES ES)、Liberty、RES 或 {redhat} {es}。
====

[IMPORTANT]
====
客户端上运行的操作系统由提供操作系统的组织支持。{sll} 由 {suse} 支持。
====

此表中图标的含意如下：

* {check} 该功能可用
* {cross} 该功能不可用
* {question} 该功能正在考虑之中，日后可能提供，也可能不提供


[cols="1,1,1,1,1,1", options="header"]
.操作系统上支持的 {sll} 功能
|===
| 功能
| {sll}{nbsp}7
| {sll}{nbsp}8
| {sll}{nbsp}9
| {sll}{nbsp}9.6 EMS
| {sll}{nbsp}10
| 客户端
| {check}
| {check}
| {check}
| {check}
| {check}
| 系统包
| {suse}
| {suse}
| {suse}
| {suse}
| {suse}
| 注册
| {check}
| {check}
| {check}
| {check}
| {check}
| 安装软件包
| {check}
| {check}
| {check}
| {check}
| {check}
| 应用补丁
| {check}
| {check}
| {check}
| {check}
| {check}
| 远程命令
| {check}
| {check}
| {check}
| {check}
| {check}
| 系统软件包状态
| {check}
| {check}
| {check}
| {check}
| {check}
| 系统自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}
| 组自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}
| 组织自定义状态
| {check}
| {check}
| {check}
| {check}
| {check}
| 系统集管理器（SSM）
| {check}
| {check}
| {check}
| {check}
| {check}
| 产品迁移
| {check} {star}
| 不适用
| 不适用
| 不适用
| 不适用
| 系统部署（PXE 引导/Kickstart）
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 系统重新部署（Kickstart）
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 联系方式
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ, Salt-SSH
| 操作链
| {check}
| {check}
| {check}
| {check}
| {check}
| 暂存 (软件包预下载)
| {check}
| {check}
| {check}
| {check}
| {check}
| 重复软件包报告
| {check}
| {check}
| {check}
| {check}
| {check}
| CVE 审计
| {check}
| {check}
| {check}
| {check}
| {check}
| SCAP 审计
| {check}
| {check}
| {check}
| {check}
| {check}
| 软件包锁定
| {check}
| {question}
| {question}
| {question}
| {question}
| 系统锁定
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 维护时段
| {check}
| {check}
| {check}
| {check}
| {check}
| 系统快照
| {cross}
| {check}
| {check}
| {check}
| {check}
| 配置文件管理
| {check}
| {check}
| {check}
| {check}
| {check}
| 快照和控制文件
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| 电源管理
| {check}
| {check}
| {check}
| {check}
| {check}
| 监视服务器
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 监控的客户端
| {check}
| {check}
| {check}
| {check}
| {check}
| Docker 构建主机
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 使用操作系统构建 Docker 镜像
| {question}
| {question}
| {question}
| {question}
| {question}
| Kiwi 构建主机
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 使用操作系统构建 Kiwi 镜像
| {cross}
| {cross}
| {cross}
| {cross}
| {cross}
| 重复性操作
| {check}
| {check}
| {check}
| {check}
| {check}
| AppStream
| 不适用
| {check}
| {check}
| {check}
| {check}
| Yomi
| 不适用
| 不适用
| 不适用
| 不适用
| 无
|===

