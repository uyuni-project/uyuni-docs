[[repos]]
= 软件储存库
:description: 配置软件储存库以从 SUSE Multi-Linux Manager 通道和客户端的本地储存库安装软件包
:revdate: 2025-02-06
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

储存库用于收集软件包。
当您可以访问软件储存库时，您可以安装该储存库提供的任何软件。
您必须至少有一个与 {productname} 中的软件通道关联的储存库，以便将客户端分配到该通道并在客户端上安装和更新软件包。

在 {productname} 中，大多数默认通道已经与正确的储存库关联。
如果您正在创建自定义通道，您需要关联一个您可以访问的储存库，或者您自己创建的储存库。

有关自定义储存库和通道的更多信息，请参见 xref:administration:custom-channels.adoc[]。



== 本地储存库位置


您可以在客户端上配置本地储存库，以提供 {productname} 通道所不提供的软件包。

[WARNING]
====
在大多数情况下，客户端系统不需要本地储存库。
本地储存库可能会导致难以确认客户端上可用的软件包。
这可能会导致安装意外的软件包。
====

初始配置期间会禁用本地储存库。

每次执行通道状态时，本地储存库将被禁用。
例如，在应用 highstate 或执行软件包操作时。

如果初始配置后本地储存库应保持启用状态，则必须为受影响的客户端设置以下 pillar：

编辑 `/srv/pillar/top.sls` 文件：

----
base:
  'minionid':
    - localrepos
----

编辑 `/srv/pillar/localrepos.sls` 文件：

----
mgr_disable_local_repos: False
----

客户端完成初始配置后，您可以在以下位置添加本地储存库：

[[local-repo-locations]]
[cols="1,1", options="header"]
.本地储存库位置
|===

ifeval::[{mlm-content} == true]
| 客户端操作系统 | 本地储存库目录
| {sles} | `/etc/zypp/repos.d`
| {opensuse} | `/etc/zypp/repos.d`
| {sles} {es} | `/etc/yum.repos.d/`
| {rhel} 及类似衍生品 | `/etc/yum.repos.d/`
| {ubuntu} | `/etc/apt/sources.list.d/`
| {debian} | `/etc/apt/sources.list.d/`
endif::[]


// SUSE Liberty Linux not available at Uyuni for now

ifeval::[{uyuni-content} == true]
| 客户端操作系统 | 本地储存库目录
| {sles} | `/etc/zypp/repos.d`
| {opensuse} | `/etc/zypp/repos.d`
| {rhel} 及类似衍生品 | `/etc/yum.repos.d/`
| {ubuntu} | `/etc/apt/sources.list.d/`
| {debian} | `/etc/apt/sources.list.d/`
endif::[]

|===
