[[supported-features-rh]]
= 支持的 {rhel} 功能
:description: 比较不同本机 Red Hat Enterprise Linux 客户端上的功能可用性，以便为您的部署策略提供参考。
:revdate: 2025-02-06
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1

ifeval::[{mlm-content} == true]

// SUSE Liberty Linux not available at Uyuni for now
下表列出了本机 {rhel} 客户端（无展开支持）上各种功能的可用性。
endif::[]

ifeval::[{uyuni-content} == true]

// SUSE Liberty Linux not available at Uyuni for now
下表列出了本机 {rhel} 客户端上各种功能的可用性。
endif::[]


[IMPORTANT]
====
客户端上运行的操作系统由提供操作系统的组织支持。{rhel} 由 {redhat} 支持。
====

此表中图标的含意如下：

* {check} 该功能可用
* {cross} 该功能不可用
* {question} 该功能正在考虑之中，日后可能提供，也可能不提供


[cols="1,1,1,1,1", options="header"]
.{rhel} 操作系统上支持的功能
|===

| 功能
| {rhela}{nbsp}7
| {rhela}{nbsp}8
| {rhela}{nbsp}9
| {rhela}{nbsp}10

| 客户端
| {check}
| {check}
| {check}
| {check}

| 系统包
| {redhat}
| {redhat}
| {redhat}
| {redhat}

| 注册
| {check}
| {check}
| {check}
| {check}

| 安装软件包
| {check}
| {check}
| {check}
| {check}

| 应用补丁
| {check}
| {check}
| {check}
| {check}

| 远程命令
| {check}
| {check}
| {check}
| {check}

| 系统包状态
| {check}
| {check}
| {check}
| {check}

| 系统自定义状态
| {check}
| {check}
| {check}
| {check}

| 组自定义状态
| {check}
| {check}
| {check}
| {check}

| 组织自定义状态
| {check}
| {check}
| {check}
| {check}

| 系统集管理器（SSM）
| {check}
| {check}
| {check}
| {check}

| 产品迁移 {star}
| 不适用
| 不适用
| 不适用
| 不适用

| 系统部署（PXE 引导/Kickstart）
| {check}
| {check}
| {check}
| {check}

| 系统重新部署（Kickstart）
| {check}
| {check}
| {check}
| {check}

| 联系方式
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH
| {check} ZeroMQ，Salt-SSH

| 与 {productname} 代理兼容
| {check}
| {check}
| {check}
| {check}

| 操作链
| {check}
| {check}
| {check}
| {check}

| 暂存（软件包的预下载）
| {check}
| {check}
| {check}
| {check}

| 重复软件包报告
| {check}
| {check}
| {check}
| {check}

| CVE 审计
| {check}
| {check}
| {check}
| {check}

| SCAP 审计
| {check}
| {check}
| {check}
| {check}

| 软件包锁定
| {check}
| {check}
| {check}
| {check}

| 系统锁定
| {cross}
| {cross}
| {cross}
| {cross}

| 维护时段
| {check}
| {check}
| {check}
| {check}

| 系统快照
| {cross}
| {cross}
| {cross}
| {cross}

| 配置文件管理
| {check}
| {check}
| {check}
| {check}

| 快照和控制文件
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步
| {check}支持 控制文件，不支持同步

| 电源管理
| {check}
| {check}
| {check}
| {check}

| 监视服务器
| {cross}
| {cross}
| {cross}
| {cross}

| 监控的客户端
| {check}
| {check}
| {check}
| {check}

| Docker 构建主机
| {check}
| {check}
| {check}
| {check}

| 使用操作系统构建 Docker 镜像
| {question}
| {question}
| {question}
| {question}

| Kiwi 构建主机
| {cross}
| {cross}
| {cross}
| {cross}

| 使用操作系统构建 Kiwi 镜像
| {cross}
| {cross}
| {cross}
| {cross}

| 重复性操作
| {check}
| {check}
| {check}
| {check}

| AppStream
| 不适用
| {check}
| {check}
| {check}

| Yomi
| 不适用
| 不适用
| 不适用
| 不适用

|===

{star} 产品迁移到匹配的 {sll} 版本。