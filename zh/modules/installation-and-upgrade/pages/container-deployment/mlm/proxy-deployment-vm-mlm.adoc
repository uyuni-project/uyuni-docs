[[proxy-install-vm]]
= {productname} 以虚拟机形式部署代理 - KVM
:description: 使用 KVM 和虚拟机管理器（virt-manager）配置 SUSE Multi-Linux Manager 5.1，将代理部署为虚拟机，确保部署成功
:revdate: 2025-06-26
:page-revdate: {revdate}
:page-author: SUSE 产品与解决方案文档团队
:page-image: https://www.suse.com/assets/img/suse-white-logo-green.svg
:page-product-name: SUSE Multi-Linux Manager
:page-product-version: 5.1
ifeval::[{uyuni-content} == true]

:noindex:
endif::[]

本章提供 {productname} {productnumber} 代理作为映像的虚拟机设置。
KVM 将与虚拟机管理器 (_virt-manager_) 结合使用，作为此安装的沙箱。



// FIXME: make this section a snippet or move it to a general intro for both,
//        KVM and VMware
== 可用映像


[IMPORTANT]
====
部署 {productname} 代理的首选方法是使用以下可用映像之一。
所有工具都包含在这些映像中，简化了部署。
====

// 2025-02-17, ke: as of today, suse-manager directory is still in use
{productname} {productnumber} 代理的映像可在 link:https://www.suse.com/download/suse-manager/[{productname} {productnumber} 虚拟机映像] 中获得。

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-warn-images-sl-micro.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-warn-images-sl-micro.adoc[]
endif::[]

[NOTE]
====
有关准备原始映像的详细信息，请参见 https://documentation.suse.com/sle-micro/6.1/html/Micro-deployment-raw-images-virtual-machines/index.html#deployment-preparing-configuration-device[]。

有关自安装映像的详细信息，请参见 https://documentation.suse.com/sle-micro/6.1/html/Micro-deployment-selfinstall-images/index.html。
====

// FIXME more archs supported?

.可用代理映像
[cols="3, 3", options="header"]
|===
| 体系结构 | 映像格式

| aarch64| qcow2, vmdk

| x86_64  | qcow2, vmdk, raw, 自安装程序

|===


[[proxy-quickstart.sect.kvm.settings]]
== 虚拟机管理器 (virt-manager) 设置

使用 *virt-manager* 创建新虚拟机时请输入以下设置。

[NOTE]
====
此表指定了最低要求。
这些适合快速测试安装，例如一个客户端的代理。

如果您想使用生产环境并需要有关磁盘空间的背景信息，请参见 xref:installation-and-upgrade:hardware-requirements.adoc[]。
====


[cols="1,1", options="header"]
|===
2+<| KVM 设置
| 安装方法 | 导入现有磁盘映像
| OS：                 | Linux
| 版本：            | {productname}-Proxy.x86_64-5.1.*.qcow2
| 内存：             | 最小值 *)
| 处理器：              | 最小值 *)
| 存储格式：    | _.qcow2_ 40 GB（默认）根分区
| 名称：               | 测试设置
| 网络             | 桥接 _br0_
|===

*) 有关最小值，请参见 xref:installation-and-upgrade:hardware-requirements.adoc#proxy-hardware-requirements[]。


[NOTE]
====
`/var/lib/containers/storage/volumes` +
最少 100 GB。
应根据您将使用的 .iso 映像、容器和引导储存库的数量来计算存储要求。
====



[[proxy-minimmal.kvm.setup]]
== 初始 KVM 设置

有关设置，请参见 xref:installation-and-upgrade:container-deployment/mlm/proxy-deployment-vm-mlm.adoc#proxy-quickstart.sect.kvm.settings[]。

.过程：升级创建初始设置
. 使用下载的 {minimal} KVM 映像创建一个新虚拟机，并选择 `Import existing disk image`。
. 配置 RAM 和 CPU 数量（至少 16 GB RAM，4 个 CPU）。
. 为您的 KVM 机器命名并选中 `Customize configuration before install` 复选框。
. 单击 btn:[开始安装] 以从映像引导。
. 在 JeOS 首次引导屏幕上选择“开始”以继续。
+

image::jeos-firstboot.png[]

. 选择键盘布局。
+

image::select-keyboard.png[]

.  接受许可协议。
+

image::accept-license-agreement.png[]

. 选择您的时区。
+

image::enter-timezone.png[]

. 输入 root 口令。
+

image::root-password.png[]

. 安装完成后，以 root 身份登录。

. 继续阅读下一节。

*) 有关最小值，请参见 xref:installation-and-upgrade:hardware-requirements.adoc#proxy-hardware-requirements[]。


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-register-proxy-mlm.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-register-proxy-mlm.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-actkey-bootstrap-proxy-mlm.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-actkey-bootstrap-proxy-mlm.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-generate_proxy_config.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-generate_proxy_config.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-transfer_proxy_config.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-transfer_proxy_config.adoc[]
endif::[]


ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-start-proxy.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../partials/snippet-start-proxy.adoc[]
endif::[]


////
[[installation-proxy-containers-services]]
== Install container services on the host system
ifeval::[{mlm-content} == true]

[NOTE]
====
`Containers Module` is required to be available for container host on a {sles} machine.
====
endif::[]

{productname} Proxy containers are using `podman` and `systemd` to run and manage all proxy containers.

First step is to install the `mgrpxy` utility provided by the package of the same name.

ifeval::[{mlm-content} == true]
The `mgrpxy` package is available in the {productname} Proxy product repositories.
endif::[]

ifeval::[{uyuni-content} == true]
The `mgrpxy` package is available in the container utils repository: pick the one matching the distribution in: https://download.opensuse.org/repositories/systemsmanagement:Uyuni:/Stable:/ContainerUtils/.
endif::[]

To install them run:

----
zypper in helm mgrpxy
----
////


=== 为服务使用自定义容器映像

默认情况下，{productname} 代理套件设置为对其每个服务使用相同的映像版本和注册表路径。
但是，可以使用以 `-tag` 和 `-image` 结尾的安装参数覆盖特定服务的默认值。

例如，可以按如下方式使用此命令：

----
mgrpxy install podman --httpd-tag 0.1.0 --httpd-image registry.opensuse.org/uyuni/proxy-httpd /path/to/config.tar.gz
----

它会调整 httpd 服务的配置文件（其中 `registry.opensuse.org/uyuni/proxy-httpds` 是要使用的映像，`0.1.0` 是版本标签），然后重新启动该服务。

要重置为默认值，请再次运行 install 命令但不要指定这些参数：

----
mgrpxy install podman /path/to/config.tar.gz
----

此命令首先将所有服务的配置重置为全局默认值，然后重新装载配置。
