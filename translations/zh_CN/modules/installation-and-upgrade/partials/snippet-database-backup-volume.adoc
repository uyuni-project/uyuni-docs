= 数据库备份卷
:revdate: 2025-07-29
:page-revdate: {revdate}

通过 [command]``mgradm migration`` 或 [command]``mgradm upgrade`` 执行服务器迁移或升级时，会创建一个包含数据库备份的卷。

当 {postgresql} 数据库有更新的版本时，必须在执行升级流程前将旧数据库存储到单独的位置。为此，[command]``mgradm`` 会动态创建卷 [path]``var-pgsql-backup``。当迁移或升级完成且用户已验证新系统可按预期正常运行后，可安全去除该卷。
