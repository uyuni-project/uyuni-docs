[NOTE]
====
经过安全加固的 {productname} 服务器主机可能会限制从 [path]``/tmp`` 文件夹执行文件。在这种情况下，可通过以下临时解决方案规避：在运行 [command]``mgradm``前，将 [literal]``TMPDIR`` 环境变量导出至其他现有路径。

例如：

[source, shell]
----
export TMPDIR=/path/to/other/tmp
----

在 {productname} 的后续更新中，相关工具将进行优化，届时无需再使用此临时解决方案。
====
