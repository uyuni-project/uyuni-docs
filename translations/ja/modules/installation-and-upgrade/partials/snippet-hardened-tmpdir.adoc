[NOTE]
====
セキュリティ強化された{productname}サーバホストでは、[path]``/tmp``フォルダからのファイル実行が制限される場合があります。このような場合は、回避策として、[command]``mgradm``を実行する前に、[literal]``TMPDIR``環境変数を別の既存のパスにエクスポートします。

例:

[source, shell]
----
export TMPDIR=/path/to/other/tmp
----

{productname}の更新では、この回避策が不要になるようにツールが変更されます。
====
