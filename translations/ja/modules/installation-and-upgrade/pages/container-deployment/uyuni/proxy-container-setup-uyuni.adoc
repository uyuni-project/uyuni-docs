[[proxy-setup-containers-uyuni]]
= コンテナ化された{productname}プロキシの設定
:revdate: 2025-06-30
:page-revdate: {revdate}
ifeval::[{mlm-content} == true]

:noindex:
endif::[]

{productname}プロキシコンテナのコンテナホストが準備された後に、コンテナの設定では、設定を完了するための追加の手順がいくつか必要になります。

.プロシージャ
. {productname}プロキシ設定アーカイブファイルを生成します
. インストールステップで準備したコンテナホストに設定アーカイブを転送し、抽出します
. [literal]``mgrpxy``でプロキシサービスを開始します

ifndef::backend-pdf[]
include::installation-and-upgrade:partial$snippet-generate_proxy_config.adoc[]
endif::[]

ifdef::backend-pdf[]
include::../../../../installation-and-upgrade/partials/snippet-generate_proxy_config.adoc[]
endif::[]


[[proxy-setup-containers-transfer-config]]
== {productname}プロキシ設定の転送


[command]``spacecmd``コマンドと{webui}経由の生成の両方の方法で、設定アーカイブが生成されます。このアーカイブは、コンテナホストで利用できるようにする必要があります。この生成されたアーカイブをコンテナホストに転送してください。



[[proxy-setup-containers-transfer-start]]
== {productname}プロキシコンテナの起動

[literal]``mgrpxy``コマンドを使用してコンテナを起動できます。

[[proc-setup-containers-setup-start]]
.プロシージャ: {productname}プロキシコンテナの起動

. コマンドを実行します。
+
----
mgrpxy start uyuni-proxy-pod
----
+
. 次のコマンドを呼び出して、すべてのコンテナが期待どおりに起動したかどうかを確認します。
+
----
podman ps
----

5つの{productname}プロキシコンテナが存在し、[literal]``proxy-pod``コンテナポッドの一部である必要があります。

* proxy-salt-broker
* proxy-httpd
* proxy-tftpd
* proxy-squid
* proxy-ssh
