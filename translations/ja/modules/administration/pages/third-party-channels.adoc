[[third-party-channels]]
= サードパーティチャンネル

{productname}には、オプションのサードパーティリポジトリの内容を、管理可能な製品の一部と同期する機能が含まれています。

一部のサードパーティGPGキーは、{productname}データベースにデフォルトで含まれていますが、含まれていないものもあります。 含まれていないサードパーティキーについては、このようなサードパーティリポジトリの同期を選択する際に、関連するキーをインポートする必要があります。

GPGキーをインポートするには、次のコマンド構文を使用し、{productname}ホストサーバでコマンドを実行します。

----
mgradm gpg add <path-to-gpg-key-file-or-URL>
----

. Example: Adding a GPG key from a file
+
----
mgradm gpg add repomd.xml.key
----

. Example: Adding a GPG key from a remote repository using a URL
+
----
mgradm gpg add https://<3rd-party-domain>/path/to/repository/repodata/repomd.xml.key
----

{productname} GPGデータベースに現在保持されているキーを一覧表示するには、次のコマンドを実行します。

----
mgrctl exec -ti -- gpg --homedir /var/lib/spacewalk/gpgdir --list-keys
----

[NOTE]
====
クライアントにパッケージをインストールする際には、クライアント自体でGPGキーを信頼する必要がある場合もあります。 このためには、GPGキーがクライアント上でも使用できる必要があります。

詳細については、xref:client-configuration:gpg-keys.adoc[]を参照してください。
====
