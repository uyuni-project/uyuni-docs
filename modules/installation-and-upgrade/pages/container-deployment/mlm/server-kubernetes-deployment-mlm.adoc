[[deploy-mlm-server]]
= {productname} {productnumber} Server Deployment on {kube}
:revdate: 2026-02-12
:page-revdate: {revdate}
ifeval::[{uyuni-content} == true]

:noindex:
endif::[]

This guide shows you how to install and configure a {productname} {productnumber} on {kube}  on {sl-micro} {microversion} or {sles} {bci-mlm}.

// OM 2026-02-12
// For the proposed structure, please add links in the 
// relevant sections to the sources I can use to start
// creating more detailed content.



== Overview

* Briefly introduce the Helm chart for deploying the server on Kubernetes.

* Highlight the cleaner structure which now splits image definitions into components.


== Prerequisites

* User's level of knowledge

* *Storage*: 
Explain that the new chart defines volumes as Persistent Volume Claims (PVCs). 
Explicitly state that the creation of the underlying PVCsis now the responsibility of the cluster administrators.

* *SSL Certificates*:
State that the Helm chart no longer handles SSL certificate generation. 
Users must create Kubernetes secrets (e.g., Uyunivert and dbcert) containing the certificates prior to deployment


== Instalation

* Details about the installation process.
  (We'll address user profile, internal team members or external customers.)

* Repository with examples of helm charts.


===  Configuration

* *Core Components*: What they are / Instructions to configure the core server images. (i.e. Procedure)

* *Optional Components*: What they are / Instructions (i.e.e procedure) for configuring sections related to optional tools such as the Hub API, CoCo, and Saline.


 

== Advanced Networking and Certificate Management

* Reference the accompanying Uyuni-chart repository for additional setup scenarios.

* Exposing the Server: 
  . Examples for configuring NGINX on RK2 to expose necessary ports.

* Certificate Automation:
   . Self-Signed: Guide users on using a self-signed chart with cert-manager and trust-manager.
   . External Vaults: Explain the open-vault chart configuration for retrieving certificates from OpenVault or Hashicorp Vault, noting its utility for sharing Certificate Authorities across multiple clusters



== Diagnostics / Troubleshooting

* Address the workflow for maintaining the deployment.

* Troubleshooting: 
  Define the process for diagnosing issues, specifically distinguishing between cluster configuration problems and Uyuni server software issues
