[[deploy-server-on-kubernetes]]
= {productname} {productnumber} Server Deployment on {kube}
:revdate: 2026-02-24
:page-revdate: {revdate}
ifeval::[{uyuni-content} == true]

:noindex:
endif::[]


{productname} introduces product deployment on {kube}, thus allowing for cleaner structure which splits image definitions into components.

This guide shows you how to install and configure a {productname} {productnumber} on {kube}  on {sl-micro} {microversion} or {sles} {bci-mlm}.



== Prerequisites

(Placeholder: This comment will be removed once we have satisfactory chapters or document structure for each point.)

[IMPORTANT]
====
TBA: Specify user's level of knowledge.
====



=== Deployment and credentials

[literal]``server-helm`` installs {productname} server.
The Helm chart does not handle SSL certificate generation.
Instead, users must create {kube} secrets containing the certificates prior to deployment.


The deployment requires four specific {kube} type secrets, each containing a username and a password key.
They are:

* [literal]``db-admin-credentials``: PostgreSQL administrator credentials.

* [literal]``db-credentials``: Credentials for the internal database user.

* [literal]``reportdb-credentials``: Credentials for the report database user.

* [literal]``admin-credentials``: Credentials for the server administrator.


The following TLS secrets are expected:

* [literal]``db-cert``: TLS certificate for the report database and needs to have the [literal]``db`` and [literal]``reportdb`` Subject Alternate Names, and the FQDN exposed to the outside world.

* [literal]``uyuni-cert``: TLS certificate for the Ingress rule and needs to have the public FQDN as Subject Alternate Name.



=== ConfigMaps

ConfigMaps expect he Root CA certificates for [literal]``db-ca`` and [literal]``uyuni-ca`` to be stored in the [literal]``ca.crt`` key.




=== Storage

The new chart defines volumes as Persistent Volume Claims (PVCs).

[WARNING]
====
* The creation of the underlying PVCs is the responsibility of the cluster administrators.

* The PVCs use the [literal]``ReadWriteOnce`` access mode.
====

The following PVCs will be created and will need to be bound to persistent volumes.

[cols="1,1", options="header"]
.Persistent Volume Claims
|===

| Persistent Volume
| Default size

|[literal]``ca-certs``
|10 MB

|[literal]``etc-apache2``
|1 MB

|[literal]``etc-cobbler``
|1 MB

|[literal]``etc-postfix``
|1 MB

|[literal]``etc-rhn``
|1 MB

|[literal]``etc-salt``
|1 MB

|[literal]``etc-sssd``
|1 MB

|[literal]``etc-sysconfig``
|20 MB

|[literal]``etc-systemd-multi``
|1 MB

|[literal]``etc-systemd-sockets``
|1 MB

|[literal]``etc-tomcat``
|1 MB

|[literal]``root``
|1 MB

|[literal]``run-salt-master``
|10 MB

|[literal]``srv-formulametadata``
|10 MB

|[literal]``srv-pillar``
|10 MB

|[literal]``srv-salt``
|10 MB

|[literal]``srv-spacewalk``
|10 MB

|[literal]``srv-susemanager``
|1 MB

|[literal]``srv-tftpboot``
|300 MB

|[literal]``srv-www``
|100 GB

|[literal]``var-cache``
|10 GB

|[literal]``var-cobbler``
|10 MB

|[literal]``var-log``
|2 GB

|[literal]``var-pgsql``
|50 GB

|[literal]``var-salt``
|10 MB

|[literal]``var-search``
|10 GB

|[literal]``var-spacewalk``
|100 GB

|===


[IMPORTANT]
====
While the default sizes are provided, it is highly recommended to change them based on the distributions you plan to synchronize.

For more information on storage requirments see xref:installation-and-upgrade:general-requirements.adoc[].
====


=== Exposing ports

{productname} requires some TCP and UDP ports to be routed to its services. 
Here is a list of the ports to map:

[cols="1,1,1,1,3", options="header"]
.List of ports
|===

| Protocol
| Port
| Service name
| Service port
| Additional comment

| TCP
| 5432
| reportdb
| 5432
|

| TCP
| 4505	
| {salt}	
| 4505
|

| TCP
| 4506
| {salt}
| 4506
|

| TCP	
| 25151	
| cobbler	
| 25151
|

| TCP	
| 9100	
| tomcat	
| 9100
|

| TCP
| 5556	
| taskomatic	
| 5556	
| Not if installed with enableMonitoring = false

| TCP	
| 5557	
| tomcat	
| 5557	
| Not if installed with enableMonitoring = false

| TCP	
| 9187	
| db	
| 9187	
| Not if installed with enableMonitoring = false

| TCP
| 9800
| taskomatic
| 9800
| Not if installed with enableMonitoring = false

| TCP
| 8001	
| taskomatic
| 8001
| Only if installed with exposeJavaDebug = true

| TCP
| 8002
| search
| 8002
| Only if installed with exposeJavaDebug = true


| TCP
| 8003
| tomcat
| 8003
| Only if installed with exposeJavaDebug = true

|===


// OM 2026-03-26: Replaced with {kube} server setup, once approved remove this section  
// == Instalation

// [NOTE]
// ====
// What to add here:

// * Details about the installation process.
//   (We'll address user profile, internal team members or external customers.)

// * Repository with examples of helm charts.

//  * [link]``https://github.com/uyuni-project/uyuni-charts``

// ====



=== {kube} server setup

To install {kube} server and configure [literal]``systemid``, follow the procedure:

.Procedure: {kube} server installation
[role=procedure]
____

. Install CA SUSE (details to come)

. Install {kube} with command:

+

----
curl -sfL https://get.rke2.io | sh -
----

. Enable and start the service (details to come)

. (Create symlinks for {kube} binaries.)

. (Set environment variables.)

. Install Helm with command: 

+

----
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-4 | bash
----

____

Continue with <<deploying-core-dependencies>>.

[[deploying-core-dependencies]]
.Procedure: Deploying core dependencies
[role=procedure]
____
. TBD: Cert-Manager & Trust-Manager: Install via Helm OCI using

. TBD: Local Path Provisioner

. TBD: {productname} Ingress: Add a new file
____



=== Configuration

(This is not the final version of the image.)

image::server-kubernetes-design.png[]

* *Core Components*: What they are / Instructions to configure the core server images. (i.e. Procedure)

* *Optional Components*: What they are / Instructions (i.e. procedure) for configuring sections related to optional tools such as the Hub API, CoCo, and Saline.


 

== Advanced Networking and Certificate Management

(Placeholder: We can have basic information here to begin with, starting with the link to helm charts.

More content can be added later.)

* (Placeholder: Add here reference the accompanying Uyuni-chart repository for additional setup scenarios.)
** Repository with examples of Helm charts.

  * [link]``https://github.com/uyuni-project/uyuni-charts``


* Exposing the Server: 
** Do we have examples for configuring NGINX on RK2 to expose necessary ports?


* Certificate Automation:
   . Self-Signed: Guide users on using a self-signed chart with cert-manager and trust-manager.
   . External Vaults: Explain the open-vault chart configuration for retrieving certificates from OpenVault or Hashicorp Vault, noting its utility for sharing Certificate Authorities across multiple clusters



== Future content: Diagnostics / Troubleshooting

* Address the workflow for maintaining the deployment.

* Troubleshooting: 
  Define the process for diagnosing issues, specifically distinguishing between cluster configuration problems and Uyuni server software issues
